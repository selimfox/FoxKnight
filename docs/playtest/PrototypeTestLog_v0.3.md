# 《狐狸骑士》Prototype Test Log v0.3

| 字段 | 内容 |
| --- | --- |
| 对应 GDD | v0.3 |
| 建立日期 | 2026-09-23 |
| 状态 | 首轮功能自测完成；人工试玩待进行 |

## 记录边界

只记录本版功能、窗口视觉、性能与主观试玩证据，分别列明。旧版通过不能代替本版验收；自动测试不能证明乐趣。设计以 `../GDD.md` 为准。

## B.1 天之锁与连斩原型（2026-09-24）

- Build：当前工作区，待首次实现提交关联；主场景 `scenes/prototype/v03/level_01.tscn`。
- 前置基线：`088c1c5` 无战前拖格操作；历史 `df9ec98` 的文档与旧测试保留。
- 关键配置：N=1；禁锢2秒/冷却4秒；续斩窗口真实2秒、倍率0.08、恢复0.12秒；弹幕/尸体其余数值见当前默认资源。
- 功能检查：Godot 4.7.1 执行 `--headless --path . --script res://tests/acceptance/v03_acceptance_runner.gd`，退出码 0，`RESULT | PASS | v0.3 acceptance | 60 checks, 0 failures`。覆盖 AC-01～10 的确定性规则，包括原生 TileMap 改画与父节点移动、动态增删雕塑、锁首目标/墙/射程/冷却、两种弹幕、反射与高速扫掠、仅直杀满足 N、三刀交替、真实时间超时、迟到友弹/尸体全灭、伤害阶段、重试与结果冻结。模拟 E → 按住瞄准 → 松开首刀 → 新点击续斩输入流程通过。
- 项目运行：`--headless --editor --path . --quit` 导入成功，退出码 0；`--path . --quit-after 3600` 实际图形窗口使用 D3D12 启动并正常退出，退出码 0。窗口截图 `build/v03_window_check.png` 可见狐狸、雕塑、弹幕、直斩预览和 HUD，没有明显缺失素材或启动崩溃。该截图未覆盖锁链禁锢、续斩窗口、尸体与结果反馈的实际视觉效果。
- 历史回归：v0.1 `tests/acceptance/acceptance_runner.gd` 通过。v0.2 既有失败见下；本版没有修改用户旧场景。
- 功能结论：核心规则按当前自动检查通过，不能据此宣布 AC 的全部视觉可读性、性能或玩法体验成立。高密度弹幕下的分段扫掠性能、Inspector 编排效率、锁链与续斩提示的窗口识别仍待单独核对。
- 体验结论：尚无创作者或外部玩家基于此 Build 的主观试玩；“主动性、连锁快感、是否好玩”均保持待验证。
- 已知边界：新雕塑为静止目标，锁提供停火窗口；关卡多起手路线、连斩节奏与尸体因果可读性须人工验收。

### 实施前回归基线

- v0.1：Godot 4.7.1 headless / Dummy audio 执行 `tests/acceptance/acceptance_runner.gd`，退出码 0，`RESULT | PASS | Prototype smoke acceptance`。日志 `work/v03_baseline_regression.log`。
- v0.2：新代码写入前执行 `tests/acceptance/v02_acceptance_runner.gd`，退出码 1，6 项失败。当前未提交的 Level01 将 InfantryB 从 (728,345) 改为 (728,144)；测试变换重编排夹具后报告该角色占地圆不完全位于地形，并引发该夹具后续检查失败。其他拖动/战斗/流程检查继续运行。本轮不修改历史夹具或用户摆位以掩盖既有失败。日志 `work/v03_v02_regression.log`。
- 运行环境：沙箱内 Godot 启动原生崩溃；相同引擎通过批准的沙箱外调用正常运行。该环境问题不计为玩法或脚本测试结果。

## 下一次创作者试玩

1. 能否通过天之锁安全接近，是否仍能选择不先用锁的路线？
2. 能否准确预判直接斩杀、弹反与尸体传递，知道为什么获续斩？
3. 强制异刀型是否带来选择，还是只能沿设计者预排路线行动？
4. 慢时间提示是否清楚、时长是否合适，是否打断战斗？
5. 连锁等待是否存在有意义的避险，是否因迟到撞击而获得意外但可解释的胜利？
6. 能否自主改 TileMap/敌人数/弹幕列表并直接运行？

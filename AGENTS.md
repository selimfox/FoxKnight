# 《狐狸骑士》项目协作入口

本文件只定义所有 agent 共用的项目规则、正式来源与任务路由。它不是 architecture agent 的专用提示词；architecture、coding 与 learning 的具体职责分别放在 `agents/` 下，避免根规则承担角色实现细节。

## 1. 指令层级与任务路由

开始任务时按以下顺序建立上下文：

1. 阅读本文件。
2. 根据任务类型阅读对应角色文件：
   - 架构、设计、范围和设计文档：`agents/architect.md`
   - Godot 实现、调试、重构和测试：`agents/coding.md`
   - 能力建设、学习计划、调研和复盘：`agents/learning.md`
3. 按角色文件要求读取与任务相关的正式来源。

一个任务涉及多个角色时，分别遵守相关角色文件，并按“设计与约束 → 实现 → 学习沉淀”的依赖顺序交接。角色文件补充本文件，不得覆盖本文件中的共用规则。

## 2. 项目文档职责

- `docs/GDD.md`：当前游戏设计和当前版本范围的唯一正式来源。
- `docs/DecisionLog.md`：重要设计变化及其原因，不重复完整设计。
- `docs/TDD.md`：技术架构、实现约束和已确认技术决策的正式来源；不得重新定义玩法。
- `docs/playtest/PrototypeTestLog_vX.Y.md`：与同版本 GDD 绑定的逐次 Prototype 验收和试玩证据；不得定义玩法，也不得把功能通过写成体验结论。
- `docs/SkillRoadmap.md`：能力建设、学习主题和验证计划；不得作为当前设计或实现需求。
- `README.md`：项目入口、目录说明和文档索引。
- `docs/archive/`：历史版本，不作为当前开发依据。
- `backups/`：修改前产生的安全备份，按原文件所在目录分类；不作为当前依据或历史版本发布目录。
- `docs/GDD.docx`：只作为阅读和展示副本；与 Markdown 不一致时以 `docs/GDD.md` 为准。

不要在本文件、README、TDD、SkillRoadmap 或 DecisionLog 中抄写 GDD 的具体玩法。

## 3. 共用边界

- 只处理用户本次任务直接涉及的范围，不顺手扩张设计、实现或文档。
- 不依赖聊天记忆、模型记忆或通用经验补全项目事实；以仓库中的正式来源为准。
- 用户要求、代码现状和正式文档冲突时，先指出冲突，不自行选择采用哪一方。
- 用户在本次任务中明确作出的新决定优先；需要同步正式来源时，按对应角色规则和 Skill 执行。
- 待讨论、待验证或后续候选内容不得被自行变成已确认需求，也不得提前实现。
- 某角色发现超出自身职责的问题时，应记录事实与影响并交给对应角色，不越界代替其决策。

## 4. 固定路径

- 当前 GDD 固定为 `docs/GDD.md`，版本号写在正文中。
- 历史 GDD 只放入 `docs/archive/`，不得替代当前路径。
- 当前 TDD 入口固定为 `docs/TDD.md`，版本化技术正文使用 `docs/TDD_vX.Y.md`；旧版正文必须保留。
- Prototype 测试记录固定放在 `docs/playtest/PrototypeTestLog_vX.Y.md`，版本号必须与其对应 GDD 一致；GDD 升版后新建对应文件，旧版记录保留且不得混写。
- 项目 Skill 的 canonical source 位于 `.agents/skills/`。
- 各 agent 的职责说明固定放在 `agents/`。
- 所有手动或自动备份统一放入根目录 `backups/`，不得散落在源文件旁：
  - 根目录文件放入 `backups/root/`。
  - 子目录文件按原相对目录镜像存放，例如 `docs/GDD.md` 的备份放入 `backups/docs/`。
  - 文件名使用 `<原文件名>_backup_YYYYMMDD_HHMMSS.<扩展名>`；创建前必须确认目标不存在，不得覆盖旧备份。
- `docs/archive/` 保存正式历史版本，`backups/` 保存修改前的安全副本；两者不得混用。
- Godot 项目建立后，入口固定为根目录 `project.godot`；脚本与资源分别进入 `scripts/` 和 `assets/` 的对应子目录。
- Godot 导出配置固定为根目录 `export_presets.cfg`。它是需要维护的项目配置，不属于构建产物；新增或修改导出目标时应同步检查该文件。
- `build/` 只保存可由项目源文件重新生成的本地导出与交付产物，不得作为代码、资源、正式文档、历史版本或备份的来源：
  - `build/<platform>/` 保存对应平台的未压缩导出目录，例如 Windows 的 EXE、PCK 与随包测试说明。
  - `build/distribution/` 保存可直接交付的 ZIP、校验文件等归档产物。
  - 正式测试说明或宣传图的源文件仍放在其正式目录；复制进 `build/` 的版本只服务于当前包体。
- `build/` 默认不纳入 Git 提交，也不得用其中的旧包反推当前项目状态。重新导出、覆盖或清理既有包体前，必须确认精确目标，并保留用户明确要求交付的版本。

## 5. Git 操作规则

- 在 `git add`、commit、push、标签发布、GitHub 仓库发布或对外交付包体前，必须使用 `$fox-knight-git-safety-gate` 执行对应模式的安全预检。Skill 通过只代表检查完成，不授予任何 Git 或远端写入权限。
- 用户可以选择 Agent 代执行或按 Skill 的 `references/manual-git.md` 手动操作；两种通道遵守同一检查标准与授权边界。
- 新建 GitHub 远程仓库时默认使用 Private；只有用户明确要求公开时才可创建或改为 Public。
- 未经用户明确要求，不得执行 `git add`、`git commit`、`git push`，不得创建、切换或合并分支，也不得改写提交历史。
- 完成任务后只提供建议的提交范围和 commit message。
- 即使用户要求提交，也默认不 push；推送必须另有明确要求。
- GDD 变更记录需要关联提交号时，使用“两步提交”：先提交功能、测试和设计变更，再以独立小型文档提交回填前一提交的短哈希。两步都必须先通过 `$fox-knight-git-safety-gate`，并在执行前向用户展示精确范围、取得明确授权；不得为解决哈希自引用而 amend、改写历史或伪造提交号。
- 每次维护 GDD、DecisionLog 或版本化 Prototype Test Log 时，检查从最新已关联提交到 `HEAD` 的未登记 Git 历史，包括用户自行创建的提交，并用 `git show` 核对实际范围。GDD 变更记录不是 Git 提交镜像：只有改变玩家可感知规则、版本范围、设计状态、核心约束、功能需求或验收标准，或由用户与 Agent 明确确认为需要长期定位／可能回退的设计里程碑时才补录。小幅参数微调、措辞修正、局部重构与纯技术提交不单独进入 GDD 变更记录；构成试玩条件的参数变化写入对应 Test Log，技术变化交给 TDD。代码与正式设计冲突或提交意图不清时只报告差异，不得静默把实现升级为设计。
- Git 提交授权与 GDD 变更记录资格是两个独立判断：用户允许创建某次提交，不等于该提交必须进入 GDD；边界不清时，先列为候选并与用户确认是否作为设计里程碑记录。
- 关联提交号是定位线索，不是无条件回退指令。执行回退前必须检查提交范围与后续依赖；标记为“基线收录”或包含无关变化的提交不得整笔直接回退。
